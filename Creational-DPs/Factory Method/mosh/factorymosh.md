# Factory

> Defer the creation of objects to subclasses.

Factory method pattern relies on _inheritence_. A lot of people don't know this! The youtubeguy's example did not use inheritance. IF mosh is correct, then the youtube guy is one of those who misunderstood this pattern!
